/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./index.html", // Asegúrate de incluir el archivo HTML principal
    "./src/**/*.{js,ts,jsx,tsx}", // Incluir todos los archivos JS, JSX, TS, TSX dentro de la carpeta src
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
